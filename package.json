{
  "name": "@reinjs/rein-schedule",
  "version": "1.0.0",
  "description": "project of reinjs",
  "author": "reinjs",
  "license": "MIT",
  "dependencies": {
    "@reinjs/rein-class": "^1.0.2",
    "@reinjs/rein-loader": "^1.0.4",
    "is-type-of": "^1.2.0",
    "node-schedule": "^1.3.0"
  },
  "devDependencies": {
    "@reinjs/rein-cli": "^1.0.6"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:reinjs/rein-schedule.git"
  },
  "plugin": {
    "name": "schedule"
  },
  "scripts": {
    "postinstall": "./node_modules/.bin/rc init @reinjs/rein-schedule",
    "dev": "NODE_ENV=dev node index.js --service.name=rein-schedule --service.tag=dev --service.port=8080"
  }
}
